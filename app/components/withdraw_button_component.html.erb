<%= button_to @label, deletion_target_path,
      method: :delete,
      class: "#{@class_name} text-red-600 border border-red-600 rounded p-1",
      form: { data: { turbo_confirm: confirm_message } }
%>
