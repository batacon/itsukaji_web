<li>
  <%= form_with(model: form_target) do |form| %>
    <%= form.date_field :date, min:, max:, onchange: submit, style: 'border: none; width: 140px;' %>
    <%= how_many_days_ago %>
    <%= form.submit '', id: form_submit_id %>
  <% end %>
</li>
